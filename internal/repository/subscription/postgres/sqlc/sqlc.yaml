version: "2"
sql:
  - engine: postgresql
    schema:
      - ../../../../../migrations/001_create_subscriptions.up.sql
    queries:
      - queries.sql
    gen:
      go:
        package: sqlc
        out: .
        sql_package: pgx/v5
        emit_json_tags: true
        overrides:
          - db_type: "uuid"
            go_type:
              type: "string"

          - db_type: "date"
            nullable: false
            go_type:
              import: "time"
              type: "Time"
          - db_type: "date"
            nullable: true
            go_type:
              import: "time"
              type: "Time"
              pointer: true

          - column: "public.subscriptions.cost"
            go_type:
              type: "int64"
