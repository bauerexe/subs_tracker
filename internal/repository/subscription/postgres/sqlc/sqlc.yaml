version: "2"
sql:
  - engine: postgresql
    schema:
      - ../../../../../migrations/001_create_subscriptions.up.sql
    queries:
      - queries.sql
    gen:
      go:
        package: sqlc
        out: .
        sql_package: pgx/v5
        emit_json_tags: true
        overrides:
          - db_type: "pg_catalog.uuid"
            go_type:
              type: string
          - db_type: "pg_catalog.date"
            nullable: true
            go_type:
              type: "*time.Time"
          - db_type: "pg_catalog.date"
            nullable: false
            go_type:
              type: time.Time
